# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: build

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    name: build-${{matrix.os}}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest, ubuntu-latest]
        #os: [windows-latest, ubuntu-latest, macOS-latest]

    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 7.0.x
    - name: Restore dependencies
      run: dotnet restore CoseSignTool/CoseSignUnitTests/CoseSignUnitTests.csproj
    - name: Build
      run: dotnet build --no-restore CoseSignTool/CoseSignUnitTests/CoseSignUnitTests.csproj
    - name: OS Test Filter
      if: ${{ "$RUNNER_OS" != "Windows" }}
      run: |
        echo "$FILTER=--filter TestCategory!=WindowsOnly"
    - name: Test
      run: dotnet test --no-build $FILTER --verbosity normal CoseSignTool/CoseSignUnitTests/CoseSignUnitTests.csproj
