# Copyright (c) Microsoft Corporation.
# Licensed under the MIT License.

[package]
name = "cosesign1-hello-world"
version = "0.1.0"
edition = "2021"
license = "MIT"

# This is a standalone consumer example and is intentionally NOT a member of the rust/ workspace.

[[bin]]
name = "cosesign1_hello_world"
path = "src/main.rs"

[dependencies]
cosesign1-common = { path = "../../cosesign1-common" }
cosesign1-mst = { path = "../../cosesign1-mst" }
cosesign1-validation = { path = "../../cosesign1-validation" }
cosesign1-x509 = { path = "../../cosesign1-x509" }

# Opt out of the parent `rust/` workspace; this is a standalone consumer example.
[workspace]
