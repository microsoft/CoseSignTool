[package]
name = "cosesigntool_plugin_api"
version = "0.1.0"
edition = { workspace = true }
license = { workspace = true }
description = "Plugin API for CoseSignTool — shared traits and IPC protocol"

[lib]
test = false

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
base64 = { workspace = true }
