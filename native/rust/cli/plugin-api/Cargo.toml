[package]
name = "cosesigntool_plugin_api"
version = "0.1.0"
edition = { workspace = true }
license = { workspace = true }
description = "Plugin API for CoseSignTool — shared traits and CBOR IPC protocol over named pipes"

[lib]
test = false

[dependencies]
cbor_primitives = { path = "../../primitives/cbor" }
cbor_primitives_everparse = { path = "../../primitives/cbor/everparse" }
